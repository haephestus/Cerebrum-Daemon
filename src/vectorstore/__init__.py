"""
vectorstore — embedding + FAISS vector-store + retrieval stack.

Top-level infrastructure package (sibling of api/, cerebrum_core/). The domain
(cerebrum_core) depends on this; this package must NOT import from cerebrum_core.
"""
